import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    title: {
        fontSize: 28,
        fontWeight: '600',
        color: '#2b2b2b',
        marginBottom: 30,
    },
});
